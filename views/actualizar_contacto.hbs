<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous" />
    <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"
        integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous" />

    <title>Actualización de datos de contacto</title>
</head>

<body class="bg-dark">
    <div id="app" class="container mt-3">
        <div class="row">
            <div class="col-md-12">

                <div class="card" style="width: 100%;">
                    <div class="card-body">
                        <h5 class="card-title">Actualización datos de contacto</h5>
                        <h6 class="card-subtitle mb-2 text-muted">{{user.nombre1}} {{user.nombre2}} {{user.apellido1}}
                            {{user.apellido2}} &nbsp; {{user.des_tipo_doc}}-{{user.ide_persona}}</h6>
                        <p class="card-text">Recuerde tener acceso al correo electrónico para recibir el token de
                            confirmación</p>
                        <div class="row">
                            <div class="col-md-3">
                                <div class="mb-3">
                                    <label for="email_personal" class="form-label">Correo electrónico personal</label>
                                    <input type="email" class="form-control" id="email_personal"
                                        placeholder="name@example.com">
                                </div>
                            </div>

                            <div class="col-md-3">
                                <div class="mb-3">
                                    <label for="celular" class="form-label">Celular</label>
                                    <input type="text" class="form-control" id="celular" placeholder="000 999 9999"
                                        value="{{user.cel_persona}}">
                                </div>
                            </div>

                            <div class="col-md-3">
                                <div class="mb-3">
                                    <label for="Dirección" class="form-label">Dirección</label>
                                    <input type="text" class="form-control" id="Dirección" placeholder="Cra 9° # 0-0"
                                        value="{{user.dir_persona}}">
                                </div>
                            </div>

                            <div class="col-md-3">
                                <div class="mb-3">
                                    <button class="btn btn-lg mt-4 btn-outline-info">Continuar</button>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>

                <!--container -->
            </div>
        </div>



    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js" type="text/javascript"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.min.js"
        integrity="sha384-nsg8ua9HAw1y0W1btsyWgBklPnCUAFLuTMS2G72MMONqmOymq585AcH49TLBQObG"
        crossorigin="anonymous"></script>



    <script type="text/javascript">
        var app = new Vue({
            el: "#app",
            delimiters: ['{[', ']}'],
            beforeMount() {
                this.consultarCargue()
            },
            data() {
                return {

                    cargando: false,
                    mostrarAlerta: false,
                    existenFacturas: false,
                    classalerta: 'alert-warning',
                    textoAlerta: "",
                    desactivarBotonSoporte: false,
                    cargues: []

                };
            },
            methods: {
                consultarCargue() {

                    console.log("El evento onchange se ejecuta");


                    fetch(
                        `{{BASE_URL}}/matricula/ListaCargueDescuento`, {
                        method: 'GET',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    })
                        .then((response) => {
                            console.log(response.status);
                            this.cargando = false;
                            return response.json();
                        })
                        .then((data) => {

                            if (!data.error) {

                                const resultdata = data.data;
                                if (resultdata.length > 0) {
                                    console.log(resultdata);
                                    this.cargues = resultdata;
                                    this.existenFacturas = true;
                                } else {
                                    //      this.classalerta= 'alert-info';
                                    //  this.mostrarAlerta = true;
                                    //  this.textoAlerta = "No se encontraron archivos cargados";

                                }


                            } else {
                                console.log("Servicio no disponible temporalmente");
                                this.mostrarAlerta = true;
                                this.classalerta = 'alert-danger';
                                this.textoAlerta = data.message;
                            }



                        });
                },
                handleFileUpload() {
                    this.file = this.$refs.file.files[0];
                    this.desactivarBotonSoporte = false;
                },

                enviarArchivo() {
                    this.cargando = true;
                    console.log("Enviar archivo file");
                    let formData = new FormData();
                    this.desactivarBotonSoporte = true;
                    formData.append('archivo', this.file);

                    fetch('{{BASE_URL}}/matricula/CargaPlantillaDescuento',
                        {
                            method: 'POST',
                            body: formData
                        }
                    ).then((response) => response.json())
                        .then(function (data) {
                            this.cargando = false;
                            if (!data.error) {
                                this.classalerta = 'alert-success';
                                this.consultarCargue();
                            } else {
                                this.classalerta = 'alert-warning';
                                this.desactivarBotonSoporte = false;

                            }
                            // 
                            this.mostrarAlerta = true;
                            this.textoAlerta = data.message;
                            // this.desactivarBotonSoporte = false;


                        }.bind(this))
                        .catch(function () {
                            console.log('FAILURE!!');
                            this.desactivarBotonSoporte = false;
                        });



                },


                elimarArchivo(codigo) {
                    fetch(
                        `{{BASE_URL}}/matricula/eliminarCargueDescuento/${codigo}`, {
                        method: 'DELETE',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    })
                        .then((response) => {

                            return response.json();
                        })
                        .then((data) => {

                            if (!data.error) {
                                this.consultarCargue();
                            }

                            this.mostrarAlerta = true;
                            this.classalerta = 'alert-info';
                            this.textoAlerta = data.message;





                        });
                },
                enviarPago(pago) {
                    this.pagoDetalle = pago;
                    console.log(pago);
                },

                procesarFormulario() { }
            },
        });

    </script>
</body>

</html>